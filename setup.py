from setuptools import setup


# bcrypt - pip install bcrypt - Good password hashing for your software and your servers
# umongo
# sanic sessoin, pip install sanic_session, sanic-session is an extension that integrate the server-backend session
# sanic session is a tool of session base on asyncio-redis, it is a good idea, But can not used in sanic
# redis-py: is a interface of redis to manipulate key-value, and we want to develop a components of session
# Sanic-Cors 0.9.9.post3 A Sanic extension adding a decorator for CORS support. Based on flask-cors by Cory Dolphin.


setup(
    name="dataplay3",
    version="0.0.1",
    author="",
    author_email="",
    url="",
    description="",
    python_requires=">=3.6",
)
